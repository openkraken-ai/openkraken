/**
 * OpenKraken Web UI App
 *
 * SvelteKit app initialization.
 */

// SvelteKit 2.x app initialization
// The actual app is configured via src/app.html and src/routes

export const prerender = false;
export const ssr = true;
